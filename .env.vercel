# Vercel deployment env for backend (ngv-backend)
DATABASE_URL=postgresql://<username>:<password>@<host>:<port>/<db>?schema=public
JWT_SECRET=your_jwt_secret_here
CORS_ORIGIN=https://ngv-black.vercel.app
PORT=4000
NODE_ENV=production
